import React from 'react'

const StartButton = ({ handleStartButtonClick }) => {

     return (
          <>
               <button onClick={handleStartButtonClick}>Start</button>
          </>
     )
}

export default StartButton
